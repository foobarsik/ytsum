import { describe, expect, it } from "vitest";
import { inboxAgentOutputSchema, learningAgentOutputSchema, researchAgentOutputSchema, recommendationSchema } from "./schemas";
const rec = { videoId: "v1", label: "Watch", reason: "Relevant", confidence: "high", evidenceBasis: "Transcript" };
describe("agent schemas", () => { it("validates recommendation contract", () => expect(recommendationSchema.parse(rec)).toEqual(rec)); it("rejects invalid confidence", () => expect(() => recommendationSchema.parse({ ...rec, confidence: "certain" })).toThrow()); it("accepts all three output shapes", () => { expect(inboxAgentOutputSchema.safeParse({ overview: "x", recommended: [rec], lowPriority: [], duplicates: [], needsTranscript: [], warnings: [] }).success).toBe(true); expect(learningAgentOutputSchema.safeParse({ overview: "x", learningPath: [rec], prerequisites: [], missingTopics: [], exercises: [], warnings: [] }).success).toBe(true); expect(researchAgentOutputSchema.safeParse({ overview: "x", themes: [], consensus: [], disagreements: [], recurringClaims: [rec], weakSignals: [], claimsToVerify: [], missingAngles: [], warnings: [] }).success).toBe(true); }); });
